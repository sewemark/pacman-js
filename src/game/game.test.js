import {expect} from 'chai';
import Game from './game';


describe('Game class tests', () => {
  it('should create Game', () => {
    //const game = new Game(undefined, undefined, undefined, undefined);
    //expect(game).to.be.defined;
  });
});

